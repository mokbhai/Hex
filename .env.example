# AWS Credentials for S3 uploads (Sparkle updates)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
S3_BUCKET=hex-updates

# Custom S3 endpoint (for Cloudflare R2, MinIO, RustFS, etc.)
# AWS_ENDPOINT=https://your-endpoint.example.com
# AWS_FORCE_PATH_STYLE=true

# Apple Developer (only needed for CI, locally uses keychain profile)
# TEAM_ID=QC99C9JE59
# APPLE_ID=your@email.com
# APPLE_ID_PASSWORD=xxxx-xxxx-xxxx-xxxx
