{
  "name": "hex-app",
  "version": "0.7.0",
  "description": "Release metadata + changelog automation for the Hex macOS app.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "release": "bun run tools/src/cli.ts release",
    "changeset": "changeset",
    "changeset:add": "changeset",
    "changeset:add-ai": "bun run tools/scripts/add-changeset.ts",
    "changeset:status": "changeset status --verbose",
    "changeset:version": "changeset version",
    "sync-changelog": "bun run tools/scripts/sync-changelog.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "@aws-sdk/client-s3": "^3.700.0",
    "chalk": "^5.3.0",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/bun": "latest"
  }
}
